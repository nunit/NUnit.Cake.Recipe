# This copy of GitVersion.yml is used in building the recipe package itself.
next-version: 1.5.0
mode: ContinuousDelivery
branches:
  main:
    label: alpha
  release:
    label: pre
  pull-request:
    label: pr
